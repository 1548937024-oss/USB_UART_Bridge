# AGENTS.md

本文件是本仓库中 Codex 执行 PCB/KiCad 相关任务时必须遵守的项目级规范。

## 项目目标

在当前仓库内建立一套可复现、可审核、可版本控制的 KiCad PCB 开发流程。
流程以 Codex、`kicad-cli`、`pcbnew` 和脚本化检查为核心，演示项目为隔离型
USB 转 UART 小板。

## 当前演示项目

项目目录：

`Codex_KiCad_USB_UART_Demo/`

核心文件：

- `hardware/USB_UART_Bridge/USB_UART_Bridge.kicad_sch`
- `hardware/USB_UART_Bridge/Power.kicad_sch`
- `hardware/USB_UART_Bridge/Communicate.kicad_sch`
- `hardware/USB_UART_Bridge/USB_UART_Bridge.kicad_pro`
- `hardware/USB_UART_Bridge/AD_Style_A4.kicad_wks`
- `hardware/USB_UART_Bridge/sym-lib-table`
- `hardware/USB_UART_Bridge/fp-lib-table`
- `requirements.yaml`
- `design_intent.yaml`
- `scripts/generate_schematic.py`
- `scripts/validate.ps1`
- `reports/erc.json`
- `generated/USB_UART_Bridge.net`
- `generated/USB_UART_Bridge_BOM.csv`
- `manufacturing/USB_UART_Bridge.pdf`

## 原理图层次结构

- 顶层图纸只放外部接口和对应电气网络。
- USB-C、USB ESD、CC 下拉、UART 接口和 VOUT 选择放在顶层图纸。
- `Power.kicad_sch` 放保险丝、隔离电源、隔离侧 LDO 和相关去耦。
- `Communicate.kicad_sch` 放 USB-UART 桥、数字隔离器和辅助状态电路。
- 跨图纸信号使用层次标签和图纸引脚连接。
- 电源网络必须使用 `power` 库中的电源符号，不得使用普通网络标签。
- 非电源网络使用实际走线连接，按曼哈顿距离建立最小生成树并优先选择较短路径。

## 环境约束

- 优先使用本机 `D:\KiCad10.0` 中的 KiCad 10。
- 如果首选路径不存在，再检测 `C:\Program Files\KiCad\10.0`。
- 自动化检查和导出优先使用 `kicad-cli`。
- 需要操作电路板对象时使用 KiCad 安装目录中的 `pcbnew` Python API。
- 不允许把 KiCad 安装路径、临时目录或用户私有路径硬编码到最终原理图数据中。
- 原理图生成后必须执行 `kicad-cli sch upgrade --force`，使文件格式与当前安装的
  KiCad 10.0 版本保持一致，禁止长期保留旧版本文件头。

## USB 转 UART 设计要求

### 隔离

- USB 数据 D+/D- 不做隔离。
- USB 供电和串口供电必须隔离。
- 隔离等级目标为 `1.5 kVrms`。
- 隔离侧和 USB 侧必须使用独立地：`ISO_GND` 和 `GND`。
- 隔离带内禁止铜皮、走线或过孔跨越。
- 隔离爬电距离不小于 `5 mm`。
- 建议在隔离带下设置板边开槽，以提高隔离可靠性。

### 信号

- 只隔离 `TXD` 和 `RXD`。
- 不保留 `DTR`、`RTS`。
- 支持连续全双工，最高 `5 Mbps`。
- UART 逻辑固定为 `3.3 V`。
- 如果需要 5 V UART 逻辑，必须另加电平转换，不能直接改变隔离侧 I/O 电压。

### 关键器件选择

- USB-UART 桥：`CH343G`，最高 `6 Mbps`。
- 数字隔离器：`ISO7721D`，双通道、最高 `100 Mbps`。
- 隔离电源：`CRE1S0505SC`，5 V 输入、5 V 隔离输出、1 W。
- 隔离侧 LDO：`AP2112K-3.3`。
- USB ESD 保护：`USBLC6-2SC6`。
- USB 接口：USB-C，USB 2.0 16P。
- 隔离输出选择：`JP1` 三针排针。

禁止在 5 Mbps 要求下重新使用 `CP2102N` 作为主桥，因为其 UART 速率不足以
覆盖该要求。

## 输出接口要求

- J2 使用 `1x4`、`2.54 mm` 排针。
- J2 引脚顺序固定为：
  1. `ISO_GND`
  2. `PWR_VOUT_ISO`
  3. `TXD`
  4. `RXD`
- `PWR_VOUT_ISO` 通过 `JP1` 选择隔离 `PWR_3V3_ISO` 或隔离 `PWR_5V_ISO`。
- 隔离输出最大负载按 `100 mA` 设计。
- `PWR_VOUT_ISO` 只改变参考电源输出，不改变 TXD/RXD 的 3.3 V 逻辑电平。

## 原理图规范

- 原理图必须使用 A4 图纸。
- 图纸模板固定使用工程内 `AD_Style_A4.kicad_wks`。该模板基于
  `ISO 5457/ISO 7200 compact`，保留四周分区编号、双层页边和右下工程标题栏，
  视觉风格应尽量接近 AD 原理图。
- `.kicad_pro` 的 `schematic.page_layout_descr_file` 和 PDF 导出必须引用同一
  图纸模板，禁止只在 GUI 中临时切换。
- 器件按信号或电流流向排列。
- 电源网络位于图面上方或左侧。
- GND 网络位于图面下方或右侧。
- 信号主流程优先从左到右，其次从上到下。
- USB 输入放在左侧。
- USB-UART 桥放在中间偏左。
- 隔离器放在中间偏右。
- 隔离输出放在右侧。
- USB-C、USB ESD 和 CC 下拉组成 J1 功能块，整体放在左下区域，并与通讯区保持
  清晰间距。
- 电源转换链放在图面上方。
- 原理图按功能划分为 USB 输入、桥接、隔离、隔离电源和输出区。
- 网络标签优先放在实际连接走线的中间位置，禁止为每个网络标签固定拉出
  `7.62 mm` 引线。
- 同一局部区域的同网引脚应使用短实线连接；跨区域或会造成交叉时，使用同名
  网络标签建立逻辑连接。孤立网络的标签引线不超过 `2.54 mm`。
- 二端器件应允许旋转 `90°/180°/270°`，使电流方向和走线方向自然，不得为了
  保持统一朝向而制造无必要的折线或交叉。
- 去耦电容应紧邻被去耦器件的对应电源和地引脚。条件允许时，电容引脚可直接与
  电源器件引脚重合，或用最短走线连接。
- 不同网络的走线不得交叉或重叠。
- 器件原点、全部器件引脚、走线端点、网络标签和 no-connect 必须落在
  KiCad 默认的 `1.27 mm` 栅格上。
- 禁止使用半格或任意小数坐标放置原理图对象。
- 电源标签朝上或左，GND 标签朝下或右。
- `PWR_FLAG` 仅用于向 ERC 声明某个电源或地网络由外部连接器或无源器件供电，
  不是电源转换或接地器件。只有当网络缺少 `power_out` 引脚时才允许放置。
- 未使用引脚必须显式放置 `no_connect`。
- 电源网络必须满足 ERC 的驱动要求。

## PCB 约束

- PCB 层数：`2` 层。
- 板厚：`1.6 mm`。
- 铜厚：`1 oz`。
- 表面处理：无铅喷锡。
- 优先使用嘉立创、立创商城常用料。
- 最小线宽初始值：`0.20 mm`。
- 最小间距初始值：`0.20 mm`。
- 最小钻孔初始值：`0.30 mm`。
- 隔离侧和 USB 侧必须独立铺铜。
- USB 差分对优先短距离、同层、等长布线，并保持参考平面连续。
- 隔离电源输入、输出和 LDO 去耦必须靠近对应器件。

## 自动化工作流

所有设计变更必须尽可能通过脚本生成，禁止直接手工修改生成物后再作为最终
源文件提交。

标准流程：

1. 修改 `requirements.yaml`。
2. 修改 `design_intent.yaml`。
3. 修改 `scripts/generate_schematic.py`。
4. 生成原理图。
5. 运行 ERC。
6. 导出网表、BOM 和 PDF。
7. 检查不同网络走线交叉数量为 0。
8. 更新版本说明后进入 PCB 阶段。

生成原理图：

```powershell
D:\KiCad10.0\bin\python.exe .\scripts\generate_schematic.py
```

执行验证和导出：

```powershell
pwsh -NoProfile -File .\scripts\validate.ps1
```

## 发布关口

- G0 环境：KiCad CLI 和 `pcbnew` 可用。
- G1 需求：需求可测试且无歧义。
- G2 架构：器件、封装、电源和复位已验证。
- G3 原理图：ERC 无错误、无警告，网表和 BOM 已生成。
- G4 布局：机械配合、隔离带和去耦位置已验证。
- G5 布线：连通性、USB 差分对和电源回流已验证。
- G6 DRC：DRC 和原理图一致性检查通过。
- G7 制造：Gerber、钻孔、坐标和 STEP 完整。
- G8 发布：干净环境可复现，输出可追溯。

## 安全和审核边界

- 不自动签署 EMC、安规、热设计或高速信号完整性结论。
- 不在没有数据手册、机械尺寸和制造规则的情况下宣称设计可生产。
- 不跨越隔离带自动布线。
- 不把 `GND` 和 `ISO_GND` 连接为同一网络。
- 不把 `PWR_VOUT_ISO` 的 5 V 选项误解为 5 V UART 逻辑。

## 当前状态

- P0 环境锁定：已完成。
- P1 需求锁定：已完成。
- P2 架构与选型：已完成。
- P3 原理图：已完成，顶层、Power 和 Communicate 均为 A4 图纸；已按 §10 完成
  网络统一命名、模块边框、器件注释、修订表和测试点，ERC 为 0 错误、0 警告。
- P4 PCB 设置与布局：尚未开始。

每次进入下一阶段前，优先检查本文件与项目中的 `requirements.yaml`、
`design_intent.yaml` 和生成脚本是否仍然一致。

## 原理图设计经验总结（2026-09-14）

### 总体优先级

- 原理图首先是给人阅读的工程语言，优先保证模块、信号流和连接关系清晰，不以走线
  最短、占用面积最小作为最高目标。
- 当“短走线”与“模块边界清晰、标签易读、功能流向明确”发生冲突时，以模块表达
  清晰为优先。
- 自动化负责规范、重复工作、可重复生成和检查；人工负责模块优先级、留白、视觉
  叙事和工程判断。

### 模块优先

- 先确定功能模块，再在模块内部放置器件。不要先按器件坐标铺满图纸，再补模块
  标签。
- 单页建议放 `2-4` 个主功能块，最多不超过 `5` 个。
- 模块之间保留明显留白；同一模块的器件应聚集，但不要求为了缩短连线而相互
  挤压。
- 模块框必须完整覆盖该模块的器件、关键引脚、去耦电容、测试点和必要注释。
  模块框不能退化成只有标题、不能覆盖全部主体器件。
- 建议在布局配置中显式定义模块、成员器件、端口、锚点和建议尺寸，再生成模块
  内部布局。不要只维护单个器件的绝对坐标。

### 连线与标签

- 模块内部优先使用短直连或短直角连接。
- 跨模块、跨图纸或会造成长距离交叉的网络，优先在同名标签或层次端口处断开，
  不拉长线穿过其他模块。
- 连线只允许水平或垂直；禁止 `45°` 折线和任意角度斜线。
- 网络标签优先放在实际走线上；独立标签可以使用一条清晰的较长引线。
- 不同标签之间必须留出可读间距，不能堆在同一位置或同一短线段上。
- 不同网络走线尽量减少交叉；若交叉不可避免，优先重新划分模块，而不是增加
  绕线。
- 连线不得穿过器件本体。

### 电源与地

- 同一电压网络全工程统一命名，使用 `PWR_5V`、`PWR_3V3` 等功能化名称。
- USB 侧地、隔离侧地和模拟地必须按用途区分，例如 `GND`、`ISO_GND`、
  `AGND`、`PGND`。
- 每个 IC 的电源引脚附近放置对应去耦电容组，优先保证器件间距和阅读清晰，
  再决定是否让电容引脚与电源引脚直接重合。
- 电源符号和地符号留在所属模块内部，避免跨页或跨模块长距离拉线。
- 电源和地网络由外部连接器或无源器件供电时，使用 `PWR_FLAG` 声明供电源；
  `PWR_FLAG` 不是电源器件，也不代表电气连接关系。

### 注释与测试

- 每个 IC 旁标注型号、关键参数和封装。
- 跳线和配置电阻要注明默认状态与可选配置。
- 关键电源、通信和调试信号放置测试点，并写明预期电压、速率或容差。
- 首页保留电源流向文字和修订表：`Rev / Date / Author / Changes`。
- 功能框、注释、测试点和器件均应位于图纸有效边框内，并保留足够安全边距。

### 自动化边界

- 自动生成适合执行命名检查、封装检查、栅格检查、边界检查、标签间距检查、
  交叉检查、ERC、网表、BOM 和 PDF 导出。
- 自动生成不适合替代模块取舍、页面重心、留白和人工审图判断。
- 手工优化后的布局如果具有长期价值，必须回写到模块布局配置或生成器中；
  不能只保留在最终 `.kicad_sch` 文件里，否则重新生成时容易丢失。
- 每次自动生成后，应比较“参考自动版”和“手工优化版”的模块位置、标签距离、
  走线长度、交叉数量和 ERC 结果，确认自动化规则是否需要调整。

### 收口检查

- ERC 零错误，警告逐条人工确认。
- 所有元件、走线、标签、注释和模块框均在 A4 有效边框内。
- 所有非电源器件封装字段完整。
- 所有标签在走线上，且标签锚点间距可读。
- 页间信号只使用层次端口，普通 Global Label 仅在确有全局必要时使用。
- 功能模块数量、信号方向、页内交叉数和模块框覆盖范围满足 §10。

## §10 原理图可读性规范（Codex 审图必检）

### 10.1 页面布局

- 信号流向统一：**左进右出、上进下出**，全工程一致
- 每页只做一个功能模块（电源树 / MCU 最小系统 / 通信接口 / 驱动功率 / 模拟传感器）
- 在保证模块边界和阅读清晰的前提下缩短关键路径：复位电路紧贴 MCU 复位脚、
  晶振靠近 XTAL、去耦电容视觉上靠近 IC 电源引脚；短走线不得破坏模块分区
- 禁止一页塞超过 5 个独立功能块

### 10.2 网络命名

- 格式：`功能_电压_序号_信号` 或 `功能_方向_目标`，如 `SPI1_SCK`、`UART2_TX_MCU`、`PWR_3V3`、`nRST_MCU`
- 禁止：`NET001`、单字母、`DATA` 等无意义/重名标签
- 低有效信号用 `n` 前缀（`nCS`、`nRST`）或 KiCad `~` 前缀自动上划线
- 全局标签（Global Label）仅用于电源/地/极少数全局信号；页间信号传递**必须**用层次端口（Hierarchical Port）

### 10.3 连线与符号

- 连线只允许水平、垂直直段；禁止 `45°` 和任意角度斜线，单页交叉不超过 4 条，超出改用标签
- 不用的引脚标注 `NC` 符号（无连接标志）
- 总线仅用于并行数据（地址/数据总线）；SPI/I2C/UART 用单线标签
- 连线不穿过元件本体
- 原理图符号引脚编号必须可见

### 10.4 电源与地

- 同一电压网络全工程统一命名：`PWR_3V3`、`PWR_5V`、`PWR_12V`，禁止混用 `VCC`/`VDD`/`3V3`
- 地按类型分区标注：`GND`（数字）、`AGND`（模拟）、`PGND`（功率）、`ISO_GND`（隔离侧）
- 电源树在首页用文字箭头标注流向：`24V_IN → [保护] → [Buck 12V] → [LDO 5V] → [LDO 3.3V]`
- 每个 IC 的电源引脚旁放去耦电容组（100nF + 10µF），原理图上用短连线或电源端口就近连接

### 10.5 模块化

- 重复电路（≥3 路相同功能）用层次子图（Sheet），主图放实例通过端口传参
- 功能分区用矩形框 + 文本框标注（如 `Buck 12V→5V (TPS54331), 2A max`）
- MCU 符号按功能分组重排引脚（电源/GPIO/模拟/通信/调试），不按物理封装顺序

### 10.6 注释与文档

- 每个 IC 旁标注：型号、关键参数、封装
- 跳线/配置电阻标注可选项：`R12: 0Ω(default)=addr 0x48, 10k=addr 0x49`
- 关键信号放测试点符号 `TPx`，标注预期值：`TP3: 3.3V ±5%`
- 原理图首页标题栏含修订表（Rev/Date/Author/Changes）

### 10.7 ERC 前自检（Codex 每次修改后必须跑）

- [ ] 所有 IC 电源引脚已连接
- [ ] 所有输入引脚有驱动源（不浮空）
- [ ] 无连接引脚已放 NC 标志
- [ ] 无孤立节点/未命名网络
- [ ] 引脚类型匹配（output 不连 output）
- [ ] 所有元件封装字段已填且校验过
- [ ] ERC 零错误（警告需逐条人工确认）

### 10.8 Codex 审图输出

把这个放在工程根目录的 `AGENTS.md` 末尾即可
